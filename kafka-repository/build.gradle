jar {
    archiveBaseName = 'kafka-sandbox'
    archiveVersion = '0.0.1'
}

dependencies {
    implementation libs.kafkaClients,
            libs.springBootStarterWeb,
            libs.swagger,
            libs.swaggerUI
}